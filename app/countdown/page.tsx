import Countdown from "@/components/Countdown";

export const metadata = {
  title: "Until I'm Holding You Again",
  description: "A countdown to the day we close the distance.",
};

export default function CountdownPage() {
  return <Countdown />;
}
